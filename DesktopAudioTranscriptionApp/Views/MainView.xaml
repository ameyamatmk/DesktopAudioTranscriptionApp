<Window x:Class="DesktopAudioTranscriptionApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopAudioTranscriptionApp.Views"
        xmlns:vm="clr-namespace:DesktopAudioTranscriptionApp.ViewModels"
        mc:Ignorable="d"
        Title="音声文字起こしツール" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Button Content="{Binding StartButtonText}"
                Command="{Binding StartStopRecordingCommand}"
                Margin="10,10,670,394"
                Width="120"
                Height="30" />
        <TextBlock Text="{Binding StatusMessage}"
                   Margin="10,395,10,10"
                   FontSize="16" />
    </Grid>
</Window>
